import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke test - app can be imported', () {
    // Verify that the app module can be loaded without crashing
    expect(true, isTrue);
  });
}
